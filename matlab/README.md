# MATLAB scaffold

`aaa.m` defines the intended AAA function signature and deliberately raises a `NotImplemented` error. The companion scripts show how a completed implementation can be called on Sphere and Rastrigin benchmarks. They are not validated optimization results.
